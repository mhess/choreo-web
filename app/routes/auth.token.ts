// import { json } from "@remix-run/node";
// import { authToken } from "../lib/token";

// export async function loader() {
//   if (!authToken.token) throw json('');
//   throw json(authToken.token);
// };
